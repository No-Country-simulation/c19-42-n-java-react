server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/adoptapp_gestionformulario?serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  application:
    name: solicitudadopcion

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka-sv:8761/eureka
  instance:
    prefer-ip-address: true